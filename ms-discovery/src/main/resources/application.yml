spring:
  application:
    name: msdiscovery
  
  security:
    user:
      name: user
      password: password

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
